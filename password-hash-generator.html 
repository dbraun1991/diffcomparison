<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Hash Generator</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #0B132E 0%, #1E3278 100%);
            color: #B7BECD;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 2rem;
        }

        .container {
            background-color: rgba(30, 50, 120, 0.4);
            backdrop-filter: blur(10px);
            border: 1px solid #1E3278;
            border-radius: 12px;
            padding: 3rem;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        h1 {
            color: #FFFFFF;
            text-align: center;
            margin-bottom: 0.5rem;
        }

        .subtitle {
            text-align: center;
            color: #9BAEEE;
            margin-bottom: 2rem;
        }

        .input-group {
            margin-bottom: 1.5rem;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            color: #FFFFFF;
            font-weight: 600;
        }

        input {
            width: 100%;
            padding: 1rem;
            background-color: rgba(11, 19, 46, 0.8);
            border: 2px solid #1E3278;
            border-radius: 8px;
            color: #FFFFFF;
            font-size: 1rem;
        }

        input:focus {
            outline: none;
            border-color: #FF5A00;
            box-shadow: 0 0 0 3px rgba(255, 90, 0, 0.1);
        }

        button {
            width: 100%;
            background-color: #FF5A00;
            color: #FFFFFF;
            border: none;
            padding: 1rem;
            font-size: 1rem;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s;
            font-weight: 600;
        }

        button:hover {
            background-color: #ff6a10;
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(255, 90, 0, 0.4);
        }

        .result {
            background-color: rgba(11, 19, 46, 0.8);
            border: 2px solid #1E3278;
            border-radius: 8px;
            padding: 1rem;
            margin-top: 1.5rem;
            word-break: break-all;
            font-family: 'Courier New', monospace;
            color: #9BAEEE;
            display: none;
        }

        .result.show {
            display: block;
        }

        .copy-button {
            margin-top: 0.5rem;
            background-color: #1E3278;
            font-size: 0.9rem;
            padding: 0.5rem;
        }

        .copy-button:hover {
            background-color: #2a4399;
        }

        .instructions {
            background-color: rgba(155, 174, 238, 0.1);
            border-left: 4px solid #FF5A00;
            padding: 1rem;
            margin-top: 2rem;
            border-radius: 4px;
        }

        .instructions h3 {
            color: #FFFFFF;
            margin-bottom: 0.5rem;
        }

        .instructions ol {
            margin-left: 1.5rem;
            line-height: 1.8;
        }

        .success-message {
            color: #4CAF50;
            text-align: center;
            margin-top: 0.5rem;
            opacity: 0;
            transition: opacity 0.3s;
        }

        .success-message.show {
            opacity: 1;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Password Hash Generator</h1>
        <p class="subtitle">Generate SHA-256 hash for your password protection</p>

        <div class="input-group">
            <label for="passwordInput">Enter Your Password:</label>
            <input 
                type="text" 
                id="passwordInput" 
                placeholder="e.g., mySecurePassword123"
            >
        </div>

        <button onclick="generateHash()">Generate Hash</button>

        <div id="result" class="result">
            <strong>Your SHA-256 Hash:</strong><br>
            <span id="hashOutput"></span>
            <button class="copy-button" onclick="copyHash()">Copy to Clipboard</button>
            <div id="successMessage" class="success-message">Copied!</div>
        </div>

        <div class="instructions">
            <h3>How to Use:</h3>
            <ol>
                <li>Enter your desired password above</li>
                <li>Click "Generate Hash"</li>
                <li>Copy the generated hash</li>
                <li>Open <code>password-auth.js</code></li>
                <li>Replace the <code>PASSWORD_HASH</code> value with your new hash</li>
            </ol>
        </div>
    </div>

    <script>
        async function generateHash() {
            const password = document.getElementById('passwordInput').value;
            
            if (!password) {
                alert('Please enter a password');
                return;
            }

            const encoder = new TextEncoder();
            const data = encoder.encode(password);
            const hashBuffer = await crypto.subtle.digest('SHA-256', data);
            const hashArray = Array.from(new Uint8Array(hashBuffer));
            const hashHex = hashArray.map(b => b.toString(16).padStart(2, '0')).join('');
            
            document.getElementById('hashOutput').textContent = hashHex;
            document.getElementById('result').classList.add('show');
        }

        async function copyHash() {
            const hash = document.getElementById('hashOutput').textContent;
            await navigator.clipboard.writeText(hash);
            
            const successMsg = document.getElementById('successMessage');
            successMsg.classList.add('show');
            setTimeout(() => {
                successMsg.classList.remove('show');
            }, 2000);
        }

        // Allow Enter key to generate hash
        document.getElementById('passwordInput').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                generateHash();
            }
        });
    </script>
</body>
</html>